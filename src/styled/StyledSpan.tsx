import styled from "styled-components"

const StyledSpan = styled.span`
  display: flex;
  align-items: center;
  position: relative;

  svg {
    cursor: pointer;
    position: absolute;
    left: -30px;
  }
`

export default StyledSpan